# Environment Configurations for Football Player/Ball Tracking Research Project

# MLflow Tracking Server URI
# Change '<host>' to the IP address or hostname of the shared tracking server.
# Set this to http://localhost:5000 when running the server locally.
MLFLOW_TRACKING_URI=http://<host>:5000
